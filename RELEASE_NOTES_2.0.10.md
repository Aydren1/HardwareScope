# HardwareScope 2.0.10

- Fixed the Settings live OSD preview so horizontal sensor layouts wrap onto additional rows instead of clipping after the first visible item.
- Kept hardware-group separators between items on the same row while avoiding stray dividers at the beginning of wrapped rows.
- Expanded preview layout calculation to size the monitor surface for every wrapped row and preserve graph space.
